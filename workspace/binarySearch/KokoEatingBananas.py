class Solution:
    def minEatingSpeed(self, piles: List[int], h: int) -> int:
        #Find max number in piles
        
        #create array k from 1 - max(piles)

        #set up binary search through k

        #for loop through piles[], check num hours required